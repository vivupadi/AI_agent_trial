apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: weather-db-pvc
  namespace: weather
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 700Mi
  storageClassName: local-path